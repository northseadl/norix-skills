interface:
  display_name: "Agent SWE Team"
  short_description: "Role-based multi-agent team with git worktree"
  default_prompt: "Assemble a role-based dev team (architect/backend/frontend/qa/reviewer) to implement [feature]. Use git worktree isolation and BLOCKED→Reply for decisions."
