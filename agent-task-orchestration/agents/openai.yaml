interface:
  display_name: "Agent 任务编排"
  short_description: "Multi-agent task decomposition and orchestration"
  default_prompt: "Decompose [requirement] into parallel tasks with DAG dependencies, then dispatch to multiple agents for execution. Monitor until completion."
