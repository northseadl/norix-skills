{
    "name": "brainstorm-cli",
    "version": "0.0.1",
    "private": true,
    "type": "module",
    "description": "Multi-agent brainstorming via async opinion collision space (Codex + Claude Code)",
    "scripts": {
        "build": "node build.mjs"
    },
    "dependencies": {
        "@openai/codex-sdk": "^0.105.0",
        "@anthropic-ai/claude-agent-sdk": "^0.2.62"
    },
    "devDependencies": {
        "esbuild": "^0.25.0"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}
