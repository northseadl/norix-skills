# React 交付 Playbook

## 目标

把方案规格转成 React 实施可执行说明。

## 必须输出

1. 页面级组件树
2. 容器组件与展示组件边界
3. 状态管理策略（本地状态/上下文/全局状态）
4. 数据请求生命周期与错误处理
5. 关键交互事件埋点建议

## 设计到工程映射

1. IA -> Route 结构
2. 交互流 -> 事件与状态迁移
3. 视觉契约 -> 组件库 token 映射

## 最低实现建议

1. 组件命名与职责单一
2. 异常态统一封装
3. 可访问性语义优先
4. 性能预算（初始加载、列表渲染、重绘）
