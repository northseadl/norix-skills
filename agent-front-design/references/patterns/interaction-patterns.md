# 交互模式库

## 模式 A：Command-Confirm

适用：高风险动作。

流程：触发 -> 二次确认 -> 执行 -> 结果反馈。

## 模式 B：Inline Edit

适用：轻量属性修改。

流程：就地编辑 -> 即时校验 -> 提交反馈。

## 模式 C：Draft-Review-Publish

适用：内容与配置发布。

流程：草稿 -> 预览 -> 审核 -> 发布。

## 统一规则

1. 反馈时延可感知
2. 错误可恢复
3. 不允许静默失败
